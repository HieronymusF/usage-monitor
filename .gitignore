# Dependencies
node_modules/

# Build output — regenerate with `npm run build`
dist/
out/

# Logs
*.log
start-log.txt

# Figma design-system plugin (separate tool, not part of the runtime monitor)
figma-plugin/

# Internal design/UX documentation (process artifacts, not release docs)
docs/

# Transient process files
design-qa.md
AGENTS.md

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Portable build artifacts
bundle/
*.exe
usage-monitor-build-cache/

# Visual diff tool — actual/diff 是每次本地生成的截图产物，不入 git
# _baseline 入 git（作为像素 diff 对比基准）
docs/ui-designs/_actual/
docs/ui-designs/_diff/
