# 行尾规范：仓库统一 LF，从根上消除 core.autocrlf 在 Windows 下制造的 CRLF/LF 假象。
# 背景：仓库无 .gitattributes 且 core.autocrlf=true 时，少数文件工作区 CRLF / blob LF
# 不一致，prettier --write 等工具会放大这种不一致（见 HANDOFF §9 L37）。
# 规则：文本文件一律 LF；二进制/脚本按需保护。

# 默认：自动识别文本文件，规范化为 LF
* text=auto eol=lf

# PowerShell 脚本：必须保留 UTF-8 BOM（见 AGENT_LESSONS L15），且不应对其做行尾规范化。
# 标记为 binary 既保护 BOM 字节，也避免任何 EOL 转换。
*.ps1 binary

# 明确的二进制资源（图标/图片/字体/安装包等），不做任何文本处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.icns binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.exe binary
*.dll binary
